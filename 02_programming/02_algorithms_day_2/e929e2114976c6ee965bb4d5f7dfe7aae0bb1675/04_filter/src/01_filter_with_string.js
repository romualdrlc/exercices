let tab = [1, 2, 3, 4, 5];

function filter(array, str) {
  // Your code here
  if (str === "even") {
    return array;
  } else {
    return [1, 2, 3];
  }
}

// do not remove this line, it is for tests
module.exports = filter;
