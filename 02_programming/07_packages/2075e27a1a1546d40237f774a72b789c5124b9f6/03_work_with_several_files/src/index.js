import * as machine from "./machine.js";

console.log(machine.fillWithLitersOfCoffee(0.5));
console.log(machine.expresso(0.08)); // => true
console.log(machine.longCoffee(0.15)); // => true
console.log(machine.longCoffee(0.15)); // => true
console.log(machine.longCoffee(0.15)); // => false
console.log(machine.expresso(0.08)); // => true
console.log(machine.expresso(0.08)); // => false
